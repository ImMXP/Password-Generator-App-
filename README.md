# Password Generator App 🔒

<p align="center">
  <b><a href="#english">English</a></b> | 
  <b><a href="#arabic">العربية</a></b>
</p>

---

## <a id="english"></a>🇬🇧 English Description

A simple and efficient Java-based Command Line Interface (CLI) application that generates random passwords with various complexity levels. This tool allows users to specify the desired password length and generates **Lowercase**, **Uppercase**, and **Mixed-case** passwords simultaneously.

### 🚀 Features
- **Customizable Length:** Users can choose a password length between **6 and 18** characters.
- **Input Validation:** Robust error handling for invalid inputs (non-integers) or out-of-range lengths.
- **Multiple Output Formats:** Generates three types of passwords in a single run:
  1. Lowercase only.
  2. Uppercase only.
  3. Mixed (Lowercase & Uppercase).
- **Randomization:** Uses `java.util.Random` to ensure unpredictable character sequences.

### 🛠️ Technologies Used
- **Language:** Java.
- **Concepts:** Arrays, Loops (`while`), Random Class, Exception Handling (`try-catch`), Logic Flow.

### 💻 How to Run

1. **Clone the repository:**
   ```bash
   git clone https://github.com/ImMXP/Password-Generator-App-

## <a id="arabic"></a>🇸🇦 الوصف بالعربية

تطبيق بسيط وفعال يعمل من خلال سطر الأوامر (CLI) ومبني بلغة الجافا لتوليد كلمات مرور عشوائية بمستويات تعقيد مختلفة. يتيح هذا البرنامج للمستخدم تحديد طول كلمة المرور، ويقوم بتوليد كلمات مرور تتكون من **حروف صغيرة**، و**حروف كبيرة**، و**مزيج بينهما** في آن واحد.

### 🚀 المميزات
- **طول مخصص:** إمكانية اختيار طول كلمة المرور ما بين **6 إلى 18** خانة.
- **التحقق من المدخلات:** نظام قوي لمعالجة الأخطاء في حال إدخال بيانات غير صحيحة (مثل الحروف بدلاً من الأرقام) أو أطوال خارج النطاق المسموح.
- **تنسيقات متعددة للمخرجات:** توليد ثلاثة أنواع من كلمات المرور في عملية تشغيل واحدة:
  1. حروف صغيرة فقط (Lowercase).
  2. حروف كبيرة فقط (Uppercase).
  3. مزيج (حروف صغيرة وكبيرة).
- **العشوائية:** استخدام مكتبة `java.util.Random` لضمان عدم القدرة على تنبؤ تسلسل الحروف.

### 🛠️ التقنيات المستخدمة
- **اللغة:** Java.
- **المفاهيم:** المصفوفات (Arrays)، الحلقات (Loops - `while`)، الفئات العشوائية (Random Class)، معالجة الاستثناءات (`try-catch`)، والمنطق البرمجي.

### 💻 طريقة التشغيل

1. **انسخ المستودع (Clone):**
   ```bash
   git clone https://github.com/ImMXP/Password-Generator-App-
